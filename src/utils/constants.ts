export const PATH = {
  register: '/register',
  login: '/login',
  logout: '/logout',
  user: '/api/user',
  auth: '/authorize',
  token: '/token',

  films: {
    popular: '/films/popular',
    last: '/films/popular',
    recommended: '/films/recommended'
  }
}

export const MODELS = {
  user: 'user',
  token: 'token'
}

export const FIELDS = {
  viewed_ids: 'viewed_ids'
}